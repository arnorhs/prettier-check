name: Test the action on push, no fetch-depth
on:
  - push

jobs:
  prettier_test:
    name: Prettier job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Testing the action
        uses: ./
        with:
          main-branch: master
